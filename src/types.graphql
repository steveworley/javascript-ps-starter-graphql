type Villain {
    id: ID!
    name: String!
    age: Int
    weight: Int
    image: String
    description: String
    powers: String
    first_appearance: String
}

type Query {
    villain(id: Int!): Villain!
    allVillains: [Villain!]!
    totalVillains: Int!
}

